# API compatibility note

`docs/CONTRACTS.md` is the current canonical contract/API document for this repo. This file remains as a compatibility entry point for older links.

Use:

- `contracts/VERSIONS.json` for schema and replay version ownership.
- `contracts/*.schema.json` for runtime payload shapes.
- `contracts/replay_record.schema.json` for trajectory-journal rows.
- `docs/PROVIDER_BOUNDARY.md` for provider transaction semantics.
- `docs/LAB.md` for lab artifacts and promotion gates.
